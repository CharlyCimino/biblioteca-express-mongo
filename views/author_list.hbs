{{> header}}

{{> sidebar}}

<h1>{{title}}</h1>
<ul>
	{{#each author_list}}
	<li><a href={{this.url}}>{{this.name}}</a> ({{dateFormatted this.date_of_birth}} -
		{{dateFormatted this.date_of_death}})
	</li>
	{{/each}}
</ul>

{{> footer}}